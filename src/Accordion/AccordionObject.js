class AccordionObject {
    cards = [];
    
    functions = {
        renderHead: null,
        renderContent: null,
        renderCard: null,
        render: null,
    };
}

export default AccordionObject;
